[phases.setup]
nixPkgs = ["php83", "php83Packages.composer"]
nixLibs = ["libxml2", "openssl", "curl"]

[phases.build]
cmds = [
    "composer install --optimize-autoloader --no-dev --no-interaction"
]

[start]
cmd = "php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"
